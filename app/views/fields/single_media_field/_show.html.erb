<% if field.data && field.data.file && field.data.file.attached? %>
	<%= image_tag url_for(field.data.file.variant(resize_to_fit: MediaObject::THUMBNAIL)), height: '100' %> 
<% else %>
	<%= 'No image' %>
<% end %>
