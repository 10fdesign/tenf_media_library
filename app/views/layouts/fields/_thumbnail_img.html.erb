<% if image.present? and image.file.attached? %>
	<img data-image-id='<%= image.id %>' src='<%= url_for image.file.variant(resize_to_fit: MediaObject::THUMBNAIL) %>' height='100' />
<% else %>
	<p>No image</p>
<% end %>
